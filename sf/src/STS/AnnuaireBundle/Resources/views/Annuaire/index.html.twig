{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Annuaire list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Prenom</th>
                <th>Telfixe</th>
                <th>Telmobile</th>
                <th>Departement</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('annuaire_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nom }}</td>
                <td>{{ entity.prenom }}</td>
                <td>{{ entity.telFixe }}</td>
                <td>{{ entity.telMobile }}</td>
                <td>{{ entity.departement }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('annuaire_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('annuaire_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('annuaire_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}

{# Form template #}

{% block form_row %}


    <div class="control-group">
        <label class="control-label">{{ form_label(form) }}</label>
        <div class="controls">
            {{ form_widget(form) }}
            <span class="help-inline error">{{ form_errors(form) }}</span>
        </div>
    </div>

{% endblock %}






